宏里面的参数使用一个美元符号 `$` 作为前缀，并使用一个**指示符**（*designator*）来注明类型：

{designators.play}

这里列出全部指示符：

* `block`
* `expr` 用于表达式
* `ident` 用于变量名或函数名
* `item`
* `pat` (**模式** *pattern*)
* `path`
* `stmt` (**语句** *statement*)
* `tt` (**令牌树** *token tree*)
* `ty` (**类型** *type*)
